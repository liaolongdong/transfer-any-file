# 更新日志

简体中文 · [English](CHANGELOG.en.md)

**Transfer Any File** 的所有值得记录的变更都写在这里。格式遵循
[Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)；`package.json` 中的版本号即扩展
manifest 的版本号，因此 `vX.Y.Z` 标签、构建产物与商店包始终指向同一次发布。

## [未发布]

### 新增

- **图片输出参数。** 当目标是 PNG、JPEG 或 WebP 时，工作台多出一行输出参数：最长边（800–4096 px，
  可接受区间 16–8192 px）始终可见，质量（40%–90%）与目标体积（20–2000 KB，可接受 1 KB–50 MB）只在
  JPEG / WebP 出现（PNG 没有质量拨盘），清晰度（96/144/200/300 DPI）只在批次里含 PDF 时出现，
  因为 DPI 不影响别的输入。
  这些参数只经由同一个出口 `utils/core/image-utils.ts` 的 `encodeCanvas()` 生效，因此能产出图片的四条
  链路（图片→图片、SVG→图片、HTML→图片、PDF→图片）对「质量 70%」的理解不可能分叉。目标体积是先走
  质量阶梯、实在压不下去才减像素，而且**是追赶的目标而非保证**：够不到时返回能做到的最小结果，而不是报错。
  参数持久化在 `fat:outputOptions`，只对图片目标生效——上一批图片留下的 800 px 上限不会悄悄降级下一批文档转换。
- **转换预设。** 当前的目标格式加输出参数可以存成一张命名卡片（最多 12 个，名称截断到 40 字符，不填
  名字就自动生成描述），显示在格式选择框上方。预设绑定的是**目标**而不是源格式——「PNG → 200 KB WebP」
  这一张，下次拖进来的是 Markdown、PDF 还是 PNG 都能用。还没有上传文件时点预设会被记住，并在文件落地后
  自动应用；如果那一批文件到不了预设的目标，会明确说出来，而不是点了没反应。存储在 `fat:presets`，
  **读取时做净化**，手工改过或旧版本留下的越界参数不会出现在拨盘上。
- **`pnpm verify:listing`** —— 把每一个要粘贴进 Chrome 应用商店后台的字段按其真实上限计量，并在
  `CHROMEWEBSTORE.md` 与 `public/_locales/*/messages.json`、`wxt.config.ts`（`default_locale` 与 `__MSG_`
  占位符）、`package.json`、`.github/repo-metadata.json` 不一致时报错。
  已接入 CI 与发布工作流。商店对超长字段是**静默截断**而不是拒绝提交，所以一个字段多写三个字符，
  就可能以半句话的形式上线。
- **`CHROMEWEBSTORE.md → 提交速查`** —— 按后台四个标签页的填写顺序排列，短字段逐字给出，长文案按小节引用，不重复粘贴。
- **大批量确认框可以自己收尾。** 那个对话框现在带一个「下次不再询问」勾选框，写入的是与偏好开关同一个
  `fat:confirmConvert`——问题在哪问就在哪答，菜单仍然是把它唤回的地方。勾上之后又取消不会写入任何东西：
  这项偏好只在批次真正确认后才落盘。
- **商店截图多出第七张。** `screen-07-presets` 把输出参数与预设芯片放进同一屏真实界面——一个 PDF、目标
  WebP，而且这个目标是点芯片选中的，不是下拉框——与其余六张一样由 `pnpm assets:capture` 生成。后台那五个
  槽位现在从七张里挑五张。
- **产品页新增「工作原理」流程图与「联系」小节。** `#how` 把一次转换内部真正跑过的六步（识别、取交集、
  置灰、寻路、执行、交付）画成一条蛇形流程，箭头都指向它在转换中真正的下一个节点，于是那 27 个被置灰的组合、
  以及 BFS 串出的多步链路（`MD → HTML → PDF`）是看得见的，不再只是一句声明。`#contact` 公布能联系到作者的渠道：
  `docs/assets/wx-qrcode/wechat-qrcode.jpg`（微信 `lld_1025`，备注「taf」）、商店公开邮箱与 issue，并在文案里写明
  这是一张添加个人的码、不是 7 天就过期的群码。页面的 `dateModified`、可见的「最后更新」行与 `sitemap.xml`
  一并改到 2026-09-15。这些都不进包体——`docs/` 是仓库文档，`pnpm build` 产物已复核不含它。
- **中文推广稿补上进群入口。** `docs/promo/wechat-article.md` 结尾放了二维码与「taf」备注，把它写成提缺少格式的地方。
  英文稿仍以 issue 为唯一渠道：微信群不是英文读者走得通的入口，两篇稿子对齐的是数字与取证口径，不是分发渠道。
- **PDF 直出 JPEG / WebP。** 此前这两个格式要经 PNG 走两步：先付一次全无损编码，再重新有损压缩，多页
  ZIP 里的每页图还都叫 `.png`。现在是三个目标各一个转换器共用同一条页循环，页面带上自己的扩展名，
  页脚路径数随之从 46 变为 48。
- **HTML→Markdown 保留表格。** Turndown 默认规则会把 `<table>` 整张丢掉，而 CSV/XLSX/JSON 往返出来的
  恰恰是表格。现在表格按 GFM 管道表序列化：行列不齐按空单元格补齐，单元格里的 `|` 与 `\` 被转义，
  内容以纯文本保留，嵌套表平铺进外层表所在行。
- **产品说明页新增两项交互动效。** `#how` 的六步流程图滚动进入视口时按链路顺序依次浮现（每步延迟
  80–400ms，纯 CSS 挂在既有 `.reveal` 观察器上，零新增 JS）；`#formats` 的格式矩阵前加了一面 14 个
  徽章的格式墙，指针 90px 范围内徽章朝指针倾斜（距离平方衰减、最大 9px），移出即回位。磁吸只绑定
  `(hover: hover) and (pointer: fine)`，`prefers-reduced-motion` 下流程节点直接钉为可见终态、磁吸
  监听器完全不挂载。指标卡的数字滚动此前已有，未重复实现。Playwright 双场景（常规 / 减弱动效）
  真实页面实测 13/13 通过，零 console 错误、零外部请求；`docs/` 不进包体，扩展行为不受影响。
- **历史删错了，可以在 5 秒内反悔。** 删除单条记录与清空全部历史原本都是点一下即刻生效、无从挽回。现在
  成功提示里带一个「撤销」按钮——之所以是撤销而不是二次确认弹窗，因为弹窗会改变原有的单击交互，而撤销
  不会：不点它就什么也没发生。可撤销的窗口就是这条提示的存活时间，同一个常量管着两头
  （`UNDO_WINDOW = 5000`），提示消失即入口消失，不会留下一个点了没反应的按钮。`ElMessage` 渲染的是
  `role="alert"`，所以同一句提示本身就是读屏播报。恢复走 `useHistory().restoreRecords()`：按 `id` 合并回
  当前列表、按时间倒序、仍然只保留最近 50 条，重复点撤销不会写出重复记录。清空之后留下的空态里也放着
  「导入」入口——导出与清空两个按钮只在有记录时存在，而刚清空的那一刻正是最需要导入的时候。
- **文件列表的变化开始说话。** 工作台原先只有转换进度有 `role="status"` 播报，拖入、追加、清空文件对读屏
  用户是静默的（WCAG 4.1.3）——文件计数变了，却没有任何东西告诉他已经变了。现在同一块区域播报「已载入
  {count} 个文件」与「已清空文件列表」，播报挂在 `update:files` 这个唯一出口上，所以按钮选文件、拖拽、
  粘贴、追加、清空五条路径共用同一份行为。转换状态优先于它：批次在跑的时候播报进度，不会插进来一句文件数。
  写入前先清空再赋值一次，因为读屏只在 DOM 真变化时出声，同样的文案重复两次会被吞掉。
- **一批文件全都认不出来时，界面给的是解释而不是空下拉框。** 目标格式选择框原先只在「识别成功、但这批文件
  没有公共目标」时提示，而一个都没识别出来时同样只剩一个空框，看不出是没拖进来还是格式不支持。现在两种死路
  各说一句，前者仍然说换目标，后者直接说这批里没有可识别的格式、请换类型。
- **对比视图的分栏条有了语义与可点面积。** 它此前只是一根 12px 宽的可视线：没有任何角色可暴露，pointer
  用户又得正好点中那条窄带。现在它带 `role="separator"` + `aria-orientation="vertical"` +
  `aria-valuenow/min/max` 与一个说明用途的标签，命中区向左右各扩 6px（合计 24px，满足 WCAG 2.5.8 的最小
  指针目标），扩出去的部分是透明伪元素，视觉与原来逐像素一致。伪元素会浮在静态的面板内容之上，因此下排的
  模式按钮补了 `position: relative`，仍然盖在命中区上、照旧可点。方向键调节此前已有，未重复实现。
- **Prettier 从「装了但没人跑」变成守卫。** `pnpm format:check` 并入 `lint:all`，而 CI 的 lint job 本来就跑
  `lint:all`，于是格式第一次成为提交前必须过的检查（`pnpm fix:all` 尾部同时接上 `pnpm format`）。首次全仓库
  `pnpm format`：16 个文件的改动**只有**格式（逐个用「HEAD 版本经 Prettier 走一遍是否等于工作区版本」核过），
  其余文件是本轮的语义改动顺带被规范化。`.prettierignore` 只挡三类东西：构建生成物、
  `fixtures/`（e2e 按字节与大小断言的转换输入，重排等于改断言）、`.qoder/`（规格与计划文档）。
  顺带修掉一个被这次重排暴露出来的脆弱断言：`scripts/check-store-listing.mjs` 用带双引号的字面量在
  `wxt.config.ts` 里找 `__MSG_extensionName__`，Prettier 把对象键的引号规范成单引号后它恒为 false——
  改成不依赖引号种类的正则，它守的是「manifest 有没有引用这两个键」，不是「那两个键被怎么格式化」。
- **`pnpm verify:numbers` —— 对外散文里的数字第一次有人对账。** 格式数、路径数、可达与可选组合数、被屏蔽的
  那 27 个怎么拆、批量与体积阈值、主题数与「6 主题 × 浅/深」的 12 组、两份商店详描的实测字符数、e2e 记录的
  断言总数，共 23 个数字全部在这一次运行里取证：枚举读 `utils/core/types.ts`，边数与闭包读
  `scripts/__baseline__/conversion-paths.json`，屏蔽数按 `conversion-policy.ts` 的两个集合并现算（两个分支相加
  不等于总数时报错，而不是接受一个凑出来的数），阈值读回 `FileUpload.vue` / `useConversion.ts` / `presets.ts` /
  `useHistory.ts` / `useRecentTargets.ts` 里的常量，详描字符数按 `verify:listing` 同一把尺子量那两个粘贴块，
  断言总数读 `scripts/e2e-test.mjs` 在一次全绿收尾时写下的那份记录。
  然后拿这些值去比对 15 份对外文档的句子。**已经漂过两次的就是这类句子**：一处详描数字在无关改动下变了，
  一处名称与限制的配比写成 33/75 而真实是 20/75。
  两类句式分开处理：句中点明是哪个数字的（「可到达 143 个组合」对「实际提供 116 个」）按该事实比对；
  「N 个组合」这种同一个形状承载多个事实的，只要求它是这些事实之一的当前值——过时或凭空写出的数字照样拦下，
  但不假装能读出句子指的是哪一个。`scripts/__baseline__/prose-number-quotes.json` 记下每份文档今天把每类句子
  引到几次，句子改写走到哪儿都查不到时，少掉的那一格就是审查看得见的 diff（`--update` 重取）。
  CHANGELOG 的 1.0.0 段与 `CHROMEWEBSTORE.md` 的版本历史、拒审记录按文件排除：那些是**当时的记录**，不是现在的承诺；
  未发布段的数字仍被比对，只是不进基线。守卫本身挂在 CI 的 lint job（与 `verify:paths` 同一层：纯静态、build 之前）。
- **断言总数由套件自己报，那 4 处对外句子从此不用人盯。** 对外文档里引用的断言总数，是唯一一条得**跑完测试套件**
  才有的数字：产品页中英各一处、两篇推广稿各一处引用它，而每加一条断言就得手工把这四处找齐——本分支上已经有六次
  提交的名字就是这次同步。现在 `scripts/e2e-test.mjs` 收尾时把当次断言数写进
  `scripts/__baseline__/e2e-assertions.json`，`verify:numbers` 读的就是这份记录，于是它与格式数、阈值受同一把尺子
  管。写记录的条件是**全绿且没被 `E2E_ONLY` 过滤**：子集跑数到的是套件的一部分，失败的跑数到的是没人该引用的
  东西，两者都不许动它。CI 上「记录与实测不一致」本身就是失败——那意味着记录和散文被提交成了两个数字；本地跑则
  先把新值写回，再提示去跑 `verify:numbers` 同步散文。句子被改写到哪儿都查不到时仍由引用基线兜住，`--update`
  之后那 4 格各多出一条。三个方向都验过：把记录改成 258 报 4 处，把产品页改成 260 报 1 处，把那句的量词「项」
  写成「处」由基线报出少掉的那一格。不进射程的仍是那份 gitignore 的公众号 HTML——它今天印着 244，
  要等下一次 `pnpm promo:wechat` 重取。
- **转出 PDF「没有文字层」这件事，在产品里也说得出。** 批次结果含 PDF 时，结果卡片下方多一行说明：转出的
  PDF 是逐页位图、不含文字层，需要图上的文字可以在 PDF 查看器里试试选中复制，而那一步由查看器完成、
  本扩展不做 OCR。起因是有用户从图片转出的 PDF 里复制出了文字，据此以为我们内置了 OCR——一条只写在
  README 里的限制，在产品内查不到，于是「图片为什么不能转 TXT」的置灰说明与被复制到的文字互相矛盾。
  这也是「有损语义在产品内零披露」那条挂账的第一个落点。判据看结果文件的格式而不是来源格式：本扩展写出的
  每一份 PDF 都是位图页（转换器只有 `addImage`，没有任何往页面写文字的路径），所以不必区分它来自图片、
  HTML 还是 DOCX。两条新断言盯着它：一条查这行字在不在、说的是什么，一条在 6 主题 × 浅/深共 12 种组合下量它压在
  `el-alert` 底色上的对比度（最差 11.34:1）——正文信息文字那道对比度闸门跑在空工作台状态下，够不到这行字。
  断言总数 242 → 244，产品页与两篇推广稿里的这个数字随之同步。
- **「你在 PDF 里复制出的文字，是查看器认出来的」这句话现在处处都说得通。** 上一条把限制写进了产品，但对外
  口径只说了一半：README、产品页与商店详描都写着「不含文字层」，却没有一处回答用户真实的观察——图片转出的
  PDF 确实能选中复制。于是那句「图片 → TXT 是置灰的」读起来像自相矛盾。三处补齐，中英各一份：README 的
  常见问题新增一问；产品页在可见 FAQ 与 JSON-LD `FAQPage` 里成对加上同一问（结构化数据与页面可见内容不一致
  是搜索引擎的处罚项），并把限制卡片里那句「也就无从搜索或提取文字」改成不承诺用户做不到的写法——它断言的
  是查看器行为，而这条行为恰恰会失败。商店详细介绍的「安装前请了解」两语各加同一从句，审核要点的对应条目
  也补了应对口径。这次改动顺带暴露：速查表与上架手册里的详描字符数（英文 7,948、中文 2,838/2,845）已经陈旧，
  真实值在上一轮改措辞时就动了——它正是新守卫该管的那类数字，于是把两份粘贴块的实测字符数（按
  `verify:listing` 同一把尺子量）加成第 21、22 个事实，并把两份上架手册纳入比对范围；四个引用点现在由它盯。
- **动图被摊平这件事，结果卡片上会说。** 批次里有文件走过「把 GIF 解码成单帧」的那一步时，结果卡片多一行：
  转成图片或 PDF 的 GIF 只保留第一帧，动画不在结果文件里，需要动画请留原文件。README 的格式表与产品页 FAQ
  一直写着「GIF 取首帧」，产品内却是沉默的——用户拿到一张静止图，没有任何地方承认过少了什么。这是「有损语义
  在产品内零披露」的第二个落点，判据挂在**边**上而不是来源格式上：`Converter.flattensInput` 只由真正过一遍
  canvas 解码的 `gif→png / jpg / webp` 与 `gif→pdf` 声明，而 `gif→html` 是把原始字节塞进 `<img>`，动画完整
  地活着，那条路必须不说。编排层把这条事实随结果带出来（`ConvertResult.lostFrames`），因为结果文件名是
  「源 basename + 新扩展名」重拼的，`sample_png_….png` 上读不出它来自 GIF。两条新断言，第二条专挑沉默：
  GIF→PNG 必须有那行，GIF→HTML 必须**没有**那行——只写 `source === GIF` 也能过第一条，过不了第二条。
  断言总数 244 → 246，产品页与两篇推广稿里的这个数字随之同步。仍未覆盖：SVG 展平（`svg→md` / `svg→docx`
  是否真的留住矢量未取证，不写没测过的话）、动图 WebP 输入（`webp→png` 同样取首帧，这条边没声明），
  以及把内嵌动图的 HTML 转成图片或 PDF——那条路确实也只剩一帧，但判据与措辞都不是这一行能承担的。
- **SVG 被展平这件事，结果卡片上也会说。** 「有损语义在产品内零披露」这条挂账的最后一支至此结了（上一说的
  「未取证」在 F-8 那轮已经取证完）：`html→docx` 与 `html→md` 把文档里的内联 `<svg>` 栅格成 PNG 再嵌入，
  图形保住了、矢量没了，而这句话此前只活在 README 与产品页里。它**不能**挂在那条边上——同一个 `html→docx`，
  带图的该说、不带图的不该说，这是文档的属性而不是路由的属性，所以由真正动手的那个转换器报出来
  （`ConvertResult.svgRasterized`），编排器逐步收集：`md→docx` 与 `svg→docx` 都是多步链
  （`md→html→docx`、`svg→html→docx`），写标志的那一步在链尾，到达界面的仍是它。计数口径是**这次真的换成了
  位图的张数**：嵌套那一层随父图进同一张位图，不单独算；栅格化失败被丢弃的那张也不算——那行字说的是「已转成
  位图写进结果文件」，图整个没了的时候它同样不成立。`html→md` 还要再回头看自己产出的字符串：`gfmTable`
  把单元格压成 `textContent`，落在 `<td>` 里的那张图根本进不了 markdown，只按输入数报就会在这里说谎
  （单元格丢图是既有行为，本轮不动它，只保证不再把它说成「保住了」）。四条新断言：`md→docx`、`svg→docx`、
  `svg→md` 必须有那行字，没有 `<svg>` 的 `sample.html → docx` 必须**没有**，且后者先要求这批真产出了
  .docx——否则「没提示」只是因为什么都没渲染。断言总数 254 → 258，产品页与两篇推广稿里的这个数字随之同步。
  仍未覆盖：动图 WebP 输入（`webp→png` 同样取首帧）与「内嵌动图的 HTML → 图片 / PDF」——措辞是 GIF 专属的，
  扩到它们得换句子，不是换判据。
- **`pnpm verify:remote-code`** —— 断言构建产物里不存在「从网络取来的代码」：无 `http(s)://….js` 字面量、
  无 `importScripts(`、无 `<script src="http…">`、无把 `import()` 拼成字符串再交给 Worker 的形状、无 `eval(`，
  外加 manifest 的 CSP 不放远程脚本源。它和离线守卫一样分两层：`--source-only` 那层只看第一方源码，能在干净检出
  （build 之前）跑；默认那层读 `.output/chrome-mv3`，缺产物即失败。已接进 CI 的 lint 与 build 两个 job，
  以及发布工作流里 **Package 之前**那一步。加它的原因见同段「修复」里那条：这类形状只存在于第三方压缩产物里，
  「源码里没有网络请求」与「manifest 只要 storage」两条断言都从结构上看不见它。

### 变更

- **标签页标题跟着界面语言走。** `<title>` 原先恒为 `Transfer Any File`，切换英文不改它——而标签页标题是
  浏览器 chrome 里唯一属于这个页面的文案，同屏其他部分都已经翻译了。现在它写成「品牌名 · 页面名」，品牌名
  两语一致（故意不译），只有后半截随 `useI18n` 变化；与 `<html lang>` 在同一次 `applyDocumentLocale()` 里
  写入，仍然在挂载之前执行，所以首帧就是对的，不会有闪一下中文标签的中间态。
- **仓库根双语文档统一为「中文主文件 + 英文 `.en.md`」。** 此前 `CONTRIBUTING` / `SECURITY` / `CHANGELOG`
  是英文占主文件名、中文挂 `*.zh-CN.md`，而 README 恰好相反——同一个仓库并存三套双语约定（第三套是
  PR/ISSUE 模板的中英行内并列）。现在 README 不再是例外：中文版接管主文件名，英文版改名 `.en.md`，
  H1 下的语言切换行随之对调，全部引用点一并迁移（两份 README、`AGENTS.md`、`.qoder/rules/wxt-rules.md` §10、
  `docs/llms.txt`、PR 模板、`release.yml`），改名用 `git mv` 完成以保留历史。**副作用**：GitHub Release 的
  正文现在取自中文 `CHANGELOG.md`——`release.yml` 的 awk 只匹配 `## [X.Y.Z]` 标题，两份格式相同所以无需改脚本，
  而文末那句跨语言指针从「中文更新日志见…」反向成「英文更新日志见 `CHANGELOG.en.md`…」。
  历史条目里残留的 `*.zh-CN.md` 属于当时的记录，保留原文不改写。
- **`CHROMEWEBSTORE.md` 整篇改为中文，正文顺序改成后台填写顺序。** 那份 899 行英文把四类内容混在一起：
  待办清单、要粘贴的文案、素材规格、政策取证。现在是提交速查 → 商店文案 → 图形与素材 → 权限与隐私申报 →
  分发与开发者信息 → 首次上架（手工步骤）→ 上架后的运营 → 审核要点 → 版本历史，而两次拒审的原文与前后
  对照挪到了文末——那是查证材料，不是每次提交都要读的正文。**12 个粘贴块逐字未动**（按新旧两份的字段标签
  重新抽取比对）。它能在带着校验的前提下中文化，靠的是同批改写 `verify:listing` 的字面量锚点：字段 marker 与
  `## 提交速查` / `## 商店文案` 边界一旦不改，脚本抛 `heading not found`、CI 直接变红。行数没有降（899 → 911），
  因为 CJK 一个字符三个字节，而粘贴块与取证表格就是地板；真正减半的是围栏外的散文：71,800 → 42,254 字符。
  **没有拆成多文件**：粘贴文案、字数断言与锚点必须待在同一份文件里，拆开就把「一处改字段」变成「两处对账」。
- **GitHub 仓库元数据从商店文档里搬出来，独立成 `.github/repo-metadata.md`。** About 描述为什么以
  `file format converter` 开头、为什么同时保留 `file-format-converter` 与 `file-converter`（一个是列表页
  顶部只有 340 星的小 topic，一个是 18.8k 星头部项目占住的大 topic）、20 个 topics 用满预算的理由，以及
  本机没装 `gh` 时的一次性 `curl` 命令——这些都不是「贴进商店后台」的材料。`.github/repo-metadata.json` 与
  `repo-meta.yml` 里指向旧节名的死引用一并改指新文件。
- **`.github` 的操作文档改为中文为主。** 四份工作流（ci / static / release / repo-meta）的注释、`::error::`
  文案与步骤汇总现在是中文；**工作流名与步骤名保持英文**，它们是 Actions 界面与状态徽章上的稳定标识，改名
  会让历史记录看起来像换了任务。两个 issue 模板的 `name:` 改成中文在前，只剩英文的 placeholder 与环境预填
  改成中文，按钮文字按 `utils/i18n/zh.ts` 的实际取值写（「开始转换」）。
- **首次打开按浏览器语言定默认语言。** 此前对所有人都是中文，英文浏览器用户要在一堆中文里找语言开关，
  界面看起来像坏了。现在只要存储里没有明确选择，就回退到 `navigator.languages`——离线扩展唯一能拿到的
  信号；已经存过的 `zh`/`en` 仍然优先。由于这个偏好只在你动手切换时才写入，从未碰过开关的旧安装里
  其实没有值：下次打开会跟随浏览器语言，切回来只需一次点击，而这一次点击从此会被记住。`<html lang>`
  同时跟随实际渲染的语言，之后每次切换都同步，而不再缺席。
- **目标格式支持输入过滤。** 14 个格式挤在一个下拉里，现在输入 `web`、`xsl` 即可收窄；没有任何匹配时
  会给出提示，而不是留一个空列表。
- **修正隐私披露口径。** 商店素材此前写着应勾选「我们不收集任何用户数据」。但该表单问的是**处理**
  （handle），而 Google 明确要求：数据只在设备本地处理或存储同样必须披露——因此本扩展的正确答案是
  「如实声明在设备本地处理文件」。旧措辞是这份文档里最大的提交风险。`docs/privacy.html` 中英文均已
  写明「收集」与「处理」的区分。
- **商店 listing 的默认语言换成中文，并且由包体决定。** 后台的「主要语言」不是设置，而是包内容的清单：
  商店只为包里真实存在的 `_locales/<语言代码>/` 开放条目页，而默认语言取 manifest 的 `default_locale`。
  此前两者都没有，所以条目只有一张英文页，中文文案根本没有可贴的地方。现在带上
  `public/_locales/zh_CN/`（`default_locale`）与 `public/_locales/en/`，各只有 `extensionName` 与
  `extensionDescription` 两个键，`wxt.config.ts` 的名称与描述改成 `__MSG_extensionName__` /
  `__MSG_extensionDescription__` 占位符。**必须在 `public/` 下**：`default_locale` 按扩展根目录解析，而 WXT
  只是把 `publicDir` 原样搬进 `.output/chrome-mv3`——这是 `public/` 里唯一该存在源码的地方（另见 `icon/`）。
  两处随之改变：Chrome 显示的扩展名与权限、错误文案按浏览器 UI 语言解析（英文系统仍看英文，没有降级），
  而 listing 的默认语言是 `Chinese (China)`、英文降级为附加本地化。**界面语言不受影响**，它由 `useI18n` 与
  `fat:locale` 决定，与 listing 语言分属两处。守卫同步换口径：`verify:listing` 现在校验两份 locale 文件的
  键集一致、且每个值与 sheet 上对应的粘贴块逐字相同；`verify:meta` 把 `package.json#description` 比对到
  `_locales/en` 的 `extensionDescription`，不再比对 `wxt.config.ts` 里的字面量。**明确记下的代价**：中文简介
  一次也没进过审核，而它现在正是被审的那一份，下一次判定也无法再区分是文案问题还是这次语言切换造成的。
- **README 主次对调：`README.md` 改为中文版，英文版挪到 `README.en.md`。** GitHub 仓库首页只渲染
  `README.md`，而本项目的主要受众读中文——也就是说首页一直在给大多数人看次要语言的那一版。所有引用点随之迁移：
  两份 README 的语言切换行与目录锚点、`AGENTS.md`、`CONTRIBUTING.md` / `CONTRIBUTING.zh-CN.md`、PR 模板、
  `CHROMEWEBSTORE.md` 的文件清单、`docs/llms.txt`、产品页页脚与素材脚本的注释。`.qoder/rules/wxt-rules.md` §10
  当时把这一条记为 `*.zh-CN.md` 命名规则的唯一例外——同日稍后整个仓库都按这个方向统一了，见本节第一条。
- **产品页结构化数据与标题** —— `<h1>` 补上品类词；JSON-LD 增加 `keywords`、`datePublished`、`WebSite`
  与 `SoftwareSourceCode` 节点；FAQ 中的网络断言收窄到「第一方源码」（评审真正能核对的那一句）；三个
  最常被引用的章节原先在页内没有任何入口，现已从页脚可达；一处与自身结构化数据互相矛盾的安装耗时已删除。
- **产品页与 `llms.txt` 的元数据。** 两条 meta description 分别为 284 与 225 半角字符，而摘要窗口只有约 155：
  于是「不上传、无账号、零网络请求」这句差异点（中文侧同理）从未出现在搜索结果里，英文那条还在单词中间被截断。
  现在两者都以搜索词与格式数开头，并在窗口内收尾；让出的格式枚举本就存在于正文、FAQ 结构化数据与 JSON-LD 中。
  `llms.txt` 写的产物体积是 3.71 MB，而 `pnpm build` 报的是 3.73 MB。
- **README** —— 一处用三文件截图却写「40 个文件」的图注；两个仓库无法取证的外部数字（第三方体积上限、
  转换耗时）。
- **manifest 简介与仓库清理。** manifest 与 package 的简介现在按三大类描述格式，不再逐一列举具体格式：
  2026-09-14 的商店审核正是因为这一字段的关键词堆砌而拒稿，而镜像它的四处（`CHROMEWEBSTORE.md` 的
  Short Description、`package.json#description`、`wxt.config.ts`、推广图）必须保持一致。
  `scripts/check-package-meta.mjs` 现在不限定引号读取该简介，Prettier 把 `wxt.config.ts` 重写成单引号
  不会再让检查失败。图标 A/B 比对用的临时目录（`.icon-compare/`）与生成它的两个临时脚本
  （`scripts/tmp-icon-compare.mjs`、`scripts/visual-history.mjs`）已删除。
- **商店文案第二次去枚举。** 2026-09-15 的审核以同一条政策再次拒稿，这次点名的是详描里 WHAT YOU CAN CONVERT
  的三条分类项目符号。两份详描改为按类别与数量说明覆盖范围，不再逐个报格式名——英文的格式名出现次数从 94 降到
  3，中文从 96 降到 3，保留的三处是两条不写 PDF 就无法成立的限制说明，以及历史导出的容器名。
  COMMON CONVERSIONS / 常用转换 整段删除，`screen-02-batch` 烧进图片的文案也去掉那三个格式名，因此全部列表
  截图与推广图都重新生成。已交付的功能没有任何变化；商店文案不再承载的逐格式矩阵，仍然发布在产品说明页上。
- **协议从 ISC 改为 MIT。** 两者授予的权利相同——宽松、无 copyleft、也都不含专利授权——但 MIT 是审核方、聚合站
  与依赖工具无需查证即可识别的 SPDX 标识，也是本仓库现在对外声明的那一个。同步更新的位置：`LICENSE`、
  `package.json#license`、两份 README（徽章、横向对比表、FAQ、许可证小节）、两份 CONTRIBUTING、
  `CHROMEWEBSTORE.md` 的中英问答、`docs/llms.txt`、两篇推广稿，以及产品说明页的两处 JSON-LD `license` 节点与四处
  可见文案。扩展运行时不读取任何协议字符串，功能、存储结构与权限都没有变化。
- **fflate 挪出首屏。** ZIP 只在四件事上用得到：多个结果打包下载、多工作表 XLSX → CSV、多页 PDF → 图片、
  以及解开用户上传的压缩包。但 `import { Zip } from 'fflate'` 写在 `composables/useConversion.ts` 顶层，
  而 `App.vue` 启动时调用 `initConverters()` 静态注册全部转换器——两条路都把 fflate 拉进了首屏 chunk。
  现在 fflate 只有一个入口：`utils/core/zip.ts` 的 `loadFflate()`（首次调用时 `import()`，之后复用同一个模块实例），
  五个调用点各自在真正要用之前 `await` 它。实测工作台首屏 JS 从 440,395 B 降到 421,532 B（−18,863 B，−4.3%）。
  **代价写在明处**：整包从 3.74 MB 变成 3.76 MB——把它切成异步 chunk 本身要额外开销，换来的是首屏不载它。
- **「文字不可选定」改写成「不含文字层」。** 前一句是对「用户能做什么」下的断言，而它不成立：文件里确实没有
  文字层，但部分 PDF 查看器会自行识别页面上的文字，用户照样能选中复制。我们写出的 PDF 不含文字层这一点从未变过，
  变的只是这句话说的是文件还是人手。同步 17 处、跨 9 个文件：`AGENTS.md` 的转换语义边界、README 中英各两条
  （限制说明与 FAQ）、`CHROMEWEBSTORE.md` 的中英商店粘贴块、`docs/index.html` 的三组中英对照（JSON-LD 问答、
  可见 FAQ 问答、限制卡片）、`docs/llms.txt` 的限制清单，以及两篇推广稿与微博文案。1.0.0 发布说明里的旧句子
  原样保留：那段记录的是当时的措辞，不是现在的承诺。
- **Markdown 解析器挪出首屏。** `components/shared/PreviewDialog.vue` 顶层写着 `import { marked } from 'marked'`，
  而这个对话框是 `FileUpload.vue` 用 `defineAsyncComponent` 挂的——异步组件被 `v-show` 常驻挂载时，它的 chunk
  在启动那一刻就已经解析完，所以「懒加载」只省下了组件自身，没省下它 import 的那 41 KB。现在 `marked` 只在
  真正要渲染 Markdown 预览的那个分支里 `await import()`，与同一个函数里既有的 `await import('dompurify')`
  是同一种写法。按 Chrome **实际取回的资源**计量（不是静态 import 闭包，两者在有 `v-show` 的异步组件上会分叉）：
  工作台首屏 JS 从 489,929 B / 24 个文件降到 448,476 B / 23 个文件（−41,453 B，−8.5%；`marked` 那条 chunk
  本身 41,468 B），3 次独立采样读数一致。整包 3,758,883 → 3,758,543 B，仍是 3.76 MB——它只是从首屏搬进按需
  chunk，一字节都没有少。**界面行为差异为零**：预览的输入输出、净化步骤、失败兜底都不变，e2e 里那条 Markdown
  预览断言读的仍是同一个 `srcdoc`（该 iframe 带 `sandbox=""`，父页面读不到它的文档，所以断言看属性而非实时 DOM）。
- **结果列表不再为了读后缀而造 `File`。** 每一行要按格式决定「预览」「复制」两个按钮是否出现，而格式是从结果
  文件名的后缀认出来的。旧写法是 `new File([], name)` 包一层再交给 `detectFormat`，只为拿回那个扩展名。
  `composables/useFileDetect.ts` 因此把「只看文件名」这一半抽成模块级纯函数 `formatFromFilename(name)`，
  `components/shared/ResultDownload.vue` 直接用它，不再假造文件对象。等价性是可证的而不是测出来的：
  `new File([], name).type` 恒为 `''`，而 `MIME_MAP` 没有 `''` 这个键，所以旧路径本来就只走扩展名那一半。
  实测（真 Chrome，与扩展运行时同一引擎）：200 行各探测一次，造 `File` 要 16.35 ms，读后缀 0.04 ms
  （单次分配 ≈82 µs）；模板每行调用两处（预览按钮的 `v-if` 与复制按钮的 `isTextResult`），于是 200 行的
  一次重渲染从 ≈33 ms 的纯分配降到不足 0.1 ms——而这条路径在转换进度每次刷新时都要跑一遍。

### 修复

- **取消的批次不再被当作完成的批次。** 中途取消时，页面上方仍然显示「转换完成」的提示，底下摆着不完整的
  结果；在第一个文件完成之前就取消，则整屏什么都不剩，像是按钮没生效。现在停止的批次会明确写成停止，
  并给出仍可用结果的计数；读屏播报同样如此。
- **取消没有反馈。** 一个耗时很长的步骤（大 PDF、很高的 HTML 页）在点击「取消转换」后仍会跑一小会儿，
  这期间按钮上的计数照常增长，于是用户会再点一次。现在按钮变为「正在取消…」并禁用自身，直到批次结束。
- **一次误刷新可以静默丢掉整批文件。** 上传的文件只存在内存里，转换中关闭或刷新标签页会无痕丢失；现在
  浏览器会先弹出确认。
- **置灰的组合仍然能跑。** `utils/core/conversion-policy.ts` 只被格式选择框查询，因此任何绕过下拉框设定
  目标的调用方（残留的旧选择、从历史复用的格式对）都能启动一个注定失败、或只会产出占位结果的转换。校验
  现在落在解析路径的唯一入口 `ConverterRegistry.resolvePath()`，任何调用方都跳不过。
- **同一天的两个批次会互相覆盖压缩包。** ZIP 文件名只带日期（`converted-2026-09-14.zip`），现在改用与
  单文件命名相同的秒级精度。
- **失败原因被丢弃。** 转换器会把错误翻译成分类 key 作为标题，代价是 pdf.js / jsPDF / canvas 真正抱怨的
  那句话没了。原始信息现在作为 `Error.cause` 保留，并展示在展开的诊断里。
- **大型 HTML→PDF 与 PDF→图片可能吃爆标签页。** 一份长文档按 2 倍像素比栅格化约为 1.3 GB 的 canvas
  缓冲，而只要元素还被引用着缓冲就不会释放——丢掉引用并不够，转换器局部作用域里还拿着它。现在 PDF→图片
  在每页字节编码完成后立刻释放该页 canvas；HTML→PDF 的源 canvas 与那张切片 canvas 要撑过整个分页循环，
  循环结束时一起释放。两种路径下，像素缓冲都不会再比转换本身活得更久。
- **PNG→PDF 缺少尺寸守卫。** `image-to-pdf.ts` 会钳制超大输入并拒绝 0×0 图片，`png-to-pdf.ts` 两者都没有：
  超大 PNG 产出无法渲染的 PDF，0×0 产出空白页。现在 PNG→PDF 仍保留逐字节无损嵌入，只在超过 canvas 上限时
  才降采样，并且量的是图片本身（`naturalWidth`）而不是受 CSS 影响的盒子。
- **经链路转出的 JPEG 被当成 PNG 嵌进 PDF。** `image-to-pdf.ts` 用 `input.type` 判断嵌入格式，而它是从上传
  文件嗅探出来的，在多步链路里那个中间 blob 根本没有类型——于是产出 JPEG 的多步路径会存下它的无损 PNG，
  把 PDF 撑大。判断依据现在改为 `from`，也就是该转换器注册时声明的格式。
- **看不见的目标体积会悄悄缩小 PNG。** `目标体积` 只在 JPEG / WebP 出现，但它的值留在 `fat:outputOptions`
  里跨越目标切换：把批次改到 PNG 后，那个 20 KB 上限仍然跟着编码器走。`encodeCanvas` 的质量阶梯本来就按
  lossy 判断，丢掉像素的那一档却不是，于是 PNG 会一路缩到 40% 去够一个面板上既看不见、也无法单独清掉的
  数字。现在目标体积与质量一样只对 lossy 容器生效，PNG 的结果与「从未调过任何参数」逐字节相同。
- **非 UTF-8 的 CSV / HTML 可能乱码或直接失败。** 回退链原本是 UTF-8 → GBK，现在是 UTF-8 → GB18030 → GBK：
  覆盖 GBK 表示不了的字符，同时让单独的 `0x80` 仍按老 Windows 工具的写法解析。这条链现在也覆盖标记语言，
  于是 GBK 编码的页面会按自身编码栅格化，而不是被画成一张乱码图。两类输入保留各自的失败策略：标记语言
  （HTML、SVG）在链上所有编码都无法覆盖字节时退回有损 UTF-8 解码——也就是它一贯的宽容行为；CSV 与 JSON
  仍然报解码失败，因为在那里，产出任何阅读器都打不开的文件，比一个可据以行动的错误更糟。
- **主按钮上的白字是硬编码的。** `#fff` 在浅色绿、橙主题上不可读；前景色现在取自 `--fat-on-primary`，
  由主题自己决定按钮文字颜色，而不是继承一个不对比的值。
- **……而文字在 12 组「主题 × 明暗」里仍有 7 组不达 AA。** 读数取自真实页面上那颗可用的按钮：不合格的全部
  落在悬停或按下态——常态填充在 12 组里都已越过 4.5:1，最差 4.70:1——因为这两个交互态跟随的是主题自身的
  明度阶梯，而不是文字。现在它们由常态填充向**远离文字**的方向推导（`assets/theme/tokens.css` 里的
  `--fat-btn-solid*`），深色模式的按钮则取页面正文色而非白色，这同时让这些填充对背后的卡片达到
  6.15–9.73:1（WCAG 1.4.11）。当前文字最差值为 4.70:1，套件会为全部 12 组分别断言常态 / 悬停 / 按下三态。
  仍未修复而只作说明的是：浅色的森林绿与活力橙按钮，其填充对卡片仍不足 3:1（最浅状态 2.21 / 2.96:1）——这两档填充
  本身就接近白色，再加深到 3:1 就要花掉文字那一侧的余量。
- **混合格式批次会显示某一个文件的路径。** 选择框下方的链路条画的是 `sourceFormats[0]` 的链路，好像它
  代表整批；实际上每个文件各自解析自己的链路，因此混批时不再显示这条。
- **上传区在为一次它不会接受的拖放高亮。** 禁用文件投放时，不再点亮拖放态。
- **端到端测试的 `chrome.storage` 一直是失效的。** `page.addInitScript()` 收到的是一个裸箭头函数，
  Playwright 会把它当表达式求值而**从不调用**——于是所有关于「偏好是否被持久化」的断言，其实都跑在一个
  没有存储的页面上，而测试依赖的固定语言守卫也从未生效。现在改为 IIFE；断言条数不变，区别是它们这次
  是在存储真正可用的情况下跑过的。
- **不可信 HTML 能在渲染时把「零网络」漏出去。** 第一方源码不发请求，但一张带远程图片 / 字体 /
  `@import` 的页面喂给 HTML→PNG 栅格化、HTML→PDF 分页或预览 iframe 时，浏览器会自己去取：`sandbox=""`
  的 srcdoc iframe 照样加载子资源，html-to-image 的 cacheBust 更是为此设计成重新拉取。现在所有渲染
  不可信 HTML 的入口先把指向远程的 url() / @import / src / srcset / poster / `<base>` / meta-refresh
  剥掉再交给渲染器，栅格化 iframe 另加 `sandbox="allow-same-origin"`（脚本依旧禁死，漏网者也没有 host
  权限可接应）。端到端测试新增一条 canary URL 的零请求哨兵与配套断言。
- **上传的 ZIP 从「解开才知道多大」改成「先记账、再解压」。** 此前必须整包解完才能量尺寸，几百 KB 的包
  能放出一两个 GB。现在压缩包本身先吃普通文件那道 100 MB 门，条目取舍在解压前就按中央目录里声明的
  未压缩体积完成：单条目超限、合计超 200 MB 或批次数超 200 的条目根本不参与解压，被截断时给出明确警示
  而不是静默丢文件。
- **CSV / JSON→CSV 可能把数据变成公式。** 以 `=`、`+`、`-`、`@`、TAB、CR 开头的字符串在 Excel / WPS
  打开时会被重新解析成公式。导出时统一给字符串单元格加 `'` 前缀（数字单元格原样），覆盖 xlsx→csv 与
  json→csv 两条链路。
- **PDF→HTML 的链接目标加了白名单。** PDF 链接注释是自由文本，HTML 转义拦不住 `javascript:` 或 `data:`
  scheme 被点击执行。现在只有 http(s) / mailto / tel 会被写成 `<a href>`。
- **格式识别存在原型链盲区。** 扩展名与 MIME 两处查表都是裸 `map[key]`，能命中原型链上的值。改为显式
  自有属性判断；DOCX 的 altChunk 恢复同时收紧了 MHT 部件的 content-type 白名单与 location 长度校验。
- **CI 的内置令牌是四条工作流里唯一没显式声明权限的。** 现在收敛为 `permissions: contents: read`，
  其余三条本就是最小声明。
- **转换中还能改目标格式。** 上传区、输出参数、预设在转换时全部禁用，唯独格式下拉框漏了：中途一改会
  清掉在跑的结果，再挂到新目标名下呈现。现在同样禁用。
- **大批量确认框可以被触发两次。** 对话框还开着时再按一次 Ctrl+Enter，会叠出第二个对话框、两个批次
  同时开跑并互相覆写进度与结果。现在对话框期间新的触发直接被忽略；对话框开着时工作区被清空或改目标，
  这个批次也会放弃，不会拿用户已经改掉的旧目标继续跑。
- **被重置的批次还有状态没写完。** 中途 reset 之后，循环仍持有几次尾部写入（结果回填、取消标志、完成
  桌面通知），可能把「已中断」的卡片画回刚清空的台面。现在批次带工作区纪元，检测到被重置就跳过尾部
  写入；reset 同时会中止在跑的批次。
- **历史记录把失败的文件也计了数。** 标签、文件数与源体积按输入全集照记，10 个文件坏了 3 个仍写成
  fileCount 10。现在整条记录以真正转换成功的集合计。
- **坏 PDF 被报成「浏览器不支持该输出格式」。** 解析失败与编码失败被同一个 `imageEncode` 包住，两个
  问题各有各的修法。现在解析失败是单独的「PDF 解析失败，文件可能已损坏」，原始库错误保留在展开诊断里。
- **空 DOCX 会「成功」。** Mammoth 什么都没产出、又没有 altChunk 可恢复时，端给用户的是一张结构完整、
  内容空白的 HTML。现在直接报「DOCX 文档内容为空，无法转换」。
- **PDF→HTML 是停不下来的黑洞。** PDF→图片早已支持逐页取消与逐页释放，文本提取这条路径却既不检查
  取消信号，也不归还 pdf.js 的每页缓存。现在两者对齐：逐页可取消，每页处理完即 `cleanup()`。
- **SVG 的 `width="100%"` 被当成 100 px。** `parseFloat` 不看单位，相对长度被截成像素，栅格出来小一个
  数量级。现在只认无单位数字与 px，其余一律落回 viewBox 或默认方形。
- **0×0 的图片会「转换成功」。** 部分解码器对截断数据不报错，只报零尺寸，canvas 照常产出空白成品。
  现在零内在尺寸按图片解码失败处理。
- **偏好面板关掉后仍留着按键监听。** popover 默认 persistent，首次打开后组件（连同录制快捷键时挂在
  document 上的捕获监听）长存：录制进行中面板被关掉，下一条按键——或 Escape——会被一个看不见的界面
  吞掉。现在面板关闭即销毁，监听只在「正在录制」期间挂载、结束立即摘除。
- **文件列表可能被慢的那次操作盖掉。** 第一个 ZIP 还在解压时再丢一次文件，会开第二轮应用流程，先返回
  的那次被后返回的覆盖。现在带代际计数，最终上屏的是最后一次请求，不是最先返回的。
- **对比视图关太快会漏一个按键监听。** 结果对比在注册 keydown 之前要等一次存储读取；组件若在这之前
  被卸载，自己的移除反而先于添加执行，document 上留下一个死处理函数，每次重新挂载再叠一个。现在注册
  同步完成，分栏位置异步回填。
- **通知开关只认真正的 `true`。** `fat:notifyOnComplete` 读出非布尔值（旧构建或手工改过的存储）时
  过去会按 truthy 当作开启；现在非 `true` 一律视为关闭。
- **英文用户仍会先看到一屏中文。** `<html lang>` 与标签页标题早就在挂载前按浏览器语言定好了，界面上的
  字符串却仍从 `zh` 兜底字典起步，要等一次 `storage.local` 往返才翻成英文，而那个「语言就绪」标记没有任何
  组件消费，所以这一帧确实上了屏。现在同一次解析结果在挂载前就写进共享语言状态——标记、标题、正文从第一帧
  起是同一个语言。
- **深色模式下，Element Plus 自己的令牌还是浅色值。** 它的深色面板挂在 `html.dark` 类上，而这个项目的深色走
  `data-mode='dark'`，于是凡是没在 `assets/theme/tokens.css` 里映射过的 `--el-*` 都照旧取浅色那套：下拉面板
  底色仍是白，而面板里的文字已经换成深色的 `#bac2de`，实测只有 1.77:1；`is-light` 提示条的底色仍是近乎白色的
  `*-light-9`，对深色卡片是 15:1 的一整块高光。现在把 `--el-bg-color-overlay`、六档 `--el-fill-color*` 与五组
  语义色的 `light-3…9` / `dark-2` 一并补齐，配方与浅色侧同构——保留色相，只把承载它的底色换成深色卡片：提示条
  文字从浅色面板的 2.04–2.80:1 变成 4.59–6.31:1，下拉文字对面板底 9.26:1。
- **Markdown / HTML 预览解析失败时，对话框一直转圈。** docx 与 xlsx 分支早有 `try/catch` 落到错误提示，
  markdown 与 html 却没有：`marked` 抛错时异常落在 watcher 里，界面停在加载态不动。两条分支合成同一个
  `try/catch`，失败就进同一句「渲染失败」，而那句提示的显示条件也从此前的「仅 docx / xlsx」放开到任何
  `renderError`。同一处另修两个无障碍问题：自定义 `#header` 忽略了 Element Plus 给的 `titleId`，而这个对话框
  没有 `title` prop，于是 `aria-labelledby` 指向一个不存在的 id，整个对话框对读屏没有名字；加载图标补上
  `role="status"`。
- **两张卡片的折叠状态会互相覆盖。** 历史卡与预设卡读写同一个 `fat:collapsedState` 映射，各自 get→改→set；
  两次写入交错时，后返回的那次带着自己读到的旧快照，把前一次的结果整份丢掉。现在写入串成一条链串行执行。
  反向也修：恢复折叠状态的那次读取还在途中时用户已经点了卡片，返回的旧值会把这次点击改回去、还把它写回存储
  ——现在交互过就不再回填。
- **预览对话框打开时，方向键被背后的分栏条抢走。** 对比视图的快捷键监听挂在 `document` 上，而预览对话框盖在
  它上面：在对话框里按 `←` / `→`，本意是操作对话框，背后的分栏却跟着动了。守卫此前只挡 `el-select` /
  `el-dropdown` / `el-popper`，现在把 `.el-overlay` / `.el-dialog` 一并列入。
- **分栏条下那颗「当前模式」按钮的字色是写死的 `#fff`。** 它跟着 `--fat-primary` 的填充走，而那个填充在浅色
  绿上只让白字达到 2.54:1，深色模式的几档柔光色上最低 1.69:1。现在取 `--fat-on-btn-solid`——由主题自己声明
  这颗填充该配哪支墨，与主按钮同一套约定。
- **给用户读的正文信息文字，对比度只有 2.33–2.56:1。** 拖放区提示、文件大小、链路箭头、状态行、页脚这些句子
  吃的是 `--fat-text-placeholder`。1.4.3 豁免的是真正的输入占位符、禁用控件，以及状态已由 ARIA 承载的装饰——
  这三样都留在原地；但上面那些是读给人看的正文，不在豁免之列。14 处改指 `--fat-text-secondary`，而这个令牌本身
  在浅色四档主题的着色底色上也只有 4.33:1（rose 的 `#fff1f2` 是整套调色板里最弱的一面，中性底色上反而是过的），
  所以先把它从 `#64748b` 加深到 `#5b6b80`（浅色 4.95–5.44:1，深色 7.37–8.42:1）再换指——只换指不改色，
  等于发一个仍然不达标的「修复」。套件新增第四组对比度断言：这五串文字在 6 主题 × 明暗共 12 组配置下逐项
  ≥ 4.5:1，本次最差 4.95:1（浅色玫瑰红下的 `.drop-text`）。读数前先放行 400ms 等 token 过渡落定——主题切换带
  0.18s 过渡，在过渡中间采样拿到的是插值色，那是一组看起来完全合理的假值（第一版就是这么读到 `rgb(187,187,193)`
  这种根本不存在的底色的）。
- **Markdown / SVG → Word 交回一个空白文档。** DOMPurify 的 `html` 档不含任何 svg 标签，而这条链的两端都在用
  它：`md-to-html` 净化 `marked` 的输出时，把 Markdown 里内联的 `<svg>` 整幅删掉；`html-to-docx` 在把 HTML
  打包成 MHT altChunk 之前又删一次。于是 `SVG → Word` 和 `Markdown → Word` 报告「转换完成」，用户拿到的是
  一个合法、能打开、里面没有图的空白页。`SVG → Markdown` 是同一个结果的另一半：turndown 没有 `<svg>` 规则，
  一张图示转出来只剩 `<text>` 节点里那几个字。修分三段——把 `svg-rasterize.ts` 的尺寸推导（viewBox 回落、
  相对单位拒读、`MAX_DIM` 缩放）抽进 `utils/core/svg-raster-common.ts`；`md-to-html` 的 profile 补 `svg` 与
  `svgFilters`；新增 `utils/core/svg-embed.ts`，在 DOCX 与 Markdown 两个边界把每个内联 `<svg>` 先栅格成
  PNG `<img>`，再走原来的净化。放开 profile 不是扩大安全面：`svg-to-html.ts` 早就用同一对档位处理用户上传的
  不可信 SVG，svg 档自带 `svgDisallowed`（`script`、`set`、`animate`、`foreignObject`、`use` 一律排除），
  `on*` 由 DOMPurify 默认剥离——两个边界此前只是不一致。顺序也不能反：先 sanitize 就没有 SVG 剩下可栅格化了。
  代价写在脸上：docx 与 md 里的这张图是位图，不是矢量。选它是取舍不是妥协——Word 对 altChunk 里
  `data:image/svg+xml` 的支持在本机无法验证（没有装 Word），而 PNG 必定渲染。没有内联 SVG 的文档走原路，
  `replaceInlineSvgWithPng` 找不到 `<svg>` 时把输入字符串原样返回。断言按 PNG 的 IHDR 尺寸取证（40×30 与
  240×140），不用通用 PNG 头：同一批夹具里另有一张 1×1 的控制图，宽松的信标会在图仍被吃掉时照样报绿。
  `Markdown → PNG` 另数结果图里的红色像素——那条链经 iframe + foreignObject 出图，「生成了一个文件」既不证明
  SVG 过了净化，也不证明它画得出来。
- **在结果编辑器里敲一个字，会把「这批做了什么」的说明抹掉。** 对比视图保存编辑时只回传 `{ blob, filename }`，
  而接收方 `updateResult` 是整条替换，于是随结果一起带出来的 `containerExt`、`lostFrames` 与本轮新增的
  `svgRasterized` 全部丢失。`lostFrames` 此前没暴露，只是因为 GIF 的结果不是可编辑文本格式；Markdown 是，
  所以 SVG 这一支一上来就走得到这条路。现在按 `{ ...props.result, blob }` 回传——编辑器只换它负责的那一项。
  这条修复现在有自己的断言（`Result Edit Keeps The Disclosures`，总数 258 → 259，对外文案里的这个数字随之同步）：
  先在 `svg→md` 的卡片上确认那行字真的在，再在编辑器里按一次空格触发防抖回传，然后断言它还在。把回传改回
  `{ blob, filename }` 重跑确实变红，而且红得不止 SVG 这一支——编辑器抹掉的是结果卡片上的全部说明。
- **一张没有动画的 GIF，不再被告知「你丢了动画」。** 上一轮的首帧披露挂在**边**上：`Converter.flattensInput`
  对每条 `gif→png / jpg / webp / pdf` 都为真，于是只有一帧的 GIF 转成 PNG，卡片上写的是一句此刻并不成立的话——
  它没有帧可丢。有没有动画是**这些字节**的属性，不是这条路由的属性，与「这份文档里有没有 `<svg>`」同构，
  所以只能由转换器去读。新增 `utils/core/animated-image.ts`：走一遍 GIF 的块结构（逻辑屏描述符、全局/局部色表、
  `0x21` 扩展、`0x2c` 图像描述符、子块链），数到第二个图像描述符即返回真，最多读 4 MB；读不懂或不是 GIF 一律
  返回假，也就是宁可不说不乱说。`flattensInput` 随之删除，改由 `image-convert.ts` 与 `image-to-pdf.ts` 在
  `convert()` 里量出来，经 `ConvertResult.lostFrames` 交给编排层按步 `||=` 汇总。文案一个字没改——它说的仍是
  GIF 与首帧，只是现在只在真丢了帧的时候说。范围只在 GIF：动画 WebP（`VP8X` 的 ANIM 位）与 APNG（`acTL`）
  在这几条路上同样丢帧，但仓库没有能在离线条件下产出、且可解码的这两类夹具，没跑过的判据不上产品，
  这件事记在 `animated-image.ts` 的范围注释里。断言三向：动图 → PNG 要求那行字在；`sample.gif`（一帧）→ PNG
  要求它不在；GIF → HTML 要求它不在（那条路嵌入原始字节，动画完好）。正向那条单独存在时守不住这次这个改动——
  把判据改回「源是 GIF 就报」它照样绿——变红的是新的一格（实测过：改回静态判据后只有它红，1/235）。总数 259 → 260。
- **导入历史之前不读大小，直接把整个文件读进来。** 「导入历史」对选中的文件先 `file.text()` 再 `JSON.parse`，
  这两步都跑在工作台那个标签页的主线程上：一个手工构造的大 JSON 会让界面先失去响应，才去问用户要不要合并。
  现在只看 `file.size`，超过 16 MB 当场拒绝并把上限说给用户。这个天花板远高于扩展自己能写出的最大导出
  （50 条记录 × 每条至多 200 个文件名），所以它挡不住任何真实的历史文件，只挡住「多大都敢读」这条路。
- **上传给商店的包里带着两处「远程托管代码」，1.0.0 因此第三次被拒（2026-09-21，违规类型：内容政策）。**
  两处都在第三方依赖的压缩产物里，而且本项目一次都走不到：jsPDF 4.2.1 的 `output('pdfobjectnewwindow')` 分支把
  `https://cdnjs.cloudflare.com/ajax/libs/pdfobject/2.1.1/pdfobject.min.js` 赋给动态创建的 `<script>` 的 `src`
  （全仓库只用 `output('blob')`）；pdf.js 的 `_createCDNWrapper` 把 `await import("<url>")` 拼成字符串塞进 Blob
  再 `new Worker(blobURL)`，而它只在 `workerSrc` 跨源时才走——我们的 `workerSrc` 是包内同源的 `.mjs` 资源。
  商店的判定不看可达性：这类路径存在就是违规，所以修法只能是让字符串从包里消失，运行期加判断没有意义。
  `wxt.config.ts` 里的 `stripRemotelyHostedCode()` 在构建期把两段整块删掉，新守卫 `pnpm verify:remote-code`
  断言产物里确实不再存在这些形状——源码层与产物层在当前 3.76 MB 的包上都是绿的。**用户可见行为零变化**：
  全量 `pnpm test:e2e` 在与被测产物同一份构建的干净运行里 260/260 通过，其中走到这两处被改依赖的场景照旧——
  MD/HTML/TXT/CSV/XLSX/SVG → PDF 仍由被裁剪过的 jsPDF 出文档，PDF → PNG / WEBP 与两页 PDF → ZIP 仍由包内
  那份 worker 解码。

## [1.0.0] - 2026-09-07

首个版本：面向 Chrome（Manifest V3）的离线文件格式转换扩展。所有转换都在你自己电脑的一个标签页里
完成——无服务器、无账号、无网络请求。

### 新增

- **转换工作台** —— 点击工具栏图标即在新标签页打开。没有弹窗，也没有侧边栏，全部操作都在那一页里。
  拖放落点覆盖整个页面而不只是上传区，拖入文件时会出现全页虚线遮罩提示落点。
- **14 种格式** —— 文档（Markdown、HTML、Word `.docx`、PDF、TXT）、数据（CSV、Excel `.xlsx`、JSON）
  与图片（PNG、JPEG、WebP、BMP、GIF、SVG）；注册表中有 46 条直接路径，经广度优先搜索共有 143 个
  可达的源→目标组合，其中目标格式下拉框实际提供 116 个：另外 27 个由 `utils/core/conversion-policy.ts`
  置灰并写明原因（图片转文本/表格需要 OCR，PDF 没有可供读取的表格结构）。
- **自动多步链路** —— Markdown → PDF 实际按 Markdown → HTML → PDF 两步串跑，路径显示在选择框下方；
  新增一个转换器即自动解锁所有经过它的路径。
- **混合批次与失败隔离** —— 不同源格式共用一个目标，只列出对批次内全部文件都可达的格式；单个文件读取
  失败不会阻断整批，失败逐条列出原因，每条还能展开看到它尝试的路径与出错的那一步，并可复制一份纯文本
  诊断信息用于提交 issue。
- **过程可控** —— 长批次可随时取消（已完成结果保留）、上一轮结果可撤销、超过约 5 个文件或 20 MB 先弹
  确认、任务在后台标签页完成时可选发送桌面通知、开始转换的 `Ctrl/⌘ + Enter` 可改绑。
- **预览与编辑** —— 源文件与结果左右对照，分隔条可拖、滚动可同步，并可切换仅原文件 / 仅结果；
  `1` / `2` / `3` 直接切换这三种视图，`←` / `→` 微调分隔条。文本类结果（Markdown、HTML、TXT、JSON、
  CSV）可就地修改后再下载，任意文本结果都能一键复制到剪贴板。分隔条位置与转换历史卡片的折叠状态都会
  跨会话保留。
- **最近使用的目标格式** —— 最近用过的 6 个目标格式会以下拉顶部的「最近使用」分组呈现，且只显示对当前
  批次仍然可选的那些。
- **批量输入输出** —— ZIP 打包按条目选择压缩策略（文本 deflate 后约小 10 倍，已压缩产物直接 stored）、
  拖入 `.zip` 自动展开并把支持的条目并入批次、多工作表逐个输出 CSV、多页 PDF 逐页输出图片、支持
  剪贴板粘贴转换（`⌘V` / `Ctrl+V`）。
- **中文表格不乱码** —— CSV 读取先试 UTF-8、失败自动回退 GBK，写出时带 BOM，国内系统导出的表格能正常
  读入，转出的 CSV 双击进 Excel 也不会变方块。
- **转换历史** —— 保留最近 50 次成功转换的元数据（文件名、格式、体积，绝不含文件内容）：可按文件名
  搜索、按源/目标筛选、一键复用格式组合、逐条删除、体积趋势小图，以及按记录 ID 合并的 JSON 导出导入。
- **个性化** —— 6 种主题色 × 浅色 / 深色 / 跟随系统，主题在挂载前应用因此不会闪错色；中英双语界面
  （默认中文）。
- **无障碍** —— 跳转至主内容链接、可见焦点环、完整 ARIA 标注与 `prefers-reduced-motion` 降级；端到端
  套件从真实页面读回两组成对颜色，在 6 主题 × 浅/深全部 12 组配置下逐主题断言（品牌文字对顶栏 4.5:1、
  焦点环对卡片 3:1）。
- **隐私即结构** —— manifest 只申请 `storage`，不声明 `host_permissions`，也不含远程代码；
  `pnpm verify:offline` 在每次 CI 中断言第一方源码不发起任何请求。
- **工程链路** —— WXT + Vue 3 + TypeScript + Element Plus（按需引入），重型转换库按转换器动态引入以保持
  首屏精简；Playwright 套件直接驱动构建产物、使用 `fixtures/` 样例；CI 覆盖 lint、构建与端到端；
  GitHub Pages 从 `docs/` 部署；发布工作流负责产出商店包并校验包内容。

### 已知限制（写在前台，不做隐藏）

- PDF 输出逐页渲染为图片，转出的 PDF 文字不可选定、不可搜索。
- PDF 输入只提取文本，原排版与内嵌图片不保留。
- 不做 OCR —— 图片 → TXT/CSV/Excel 在选择框中置灰并写明原因，因为内置 OCR 模型会多出几十 MB 并引入
  模型下载，与离线承诺冲突。
- BMP、GIF、SVG 只能作为输入，浏览器不提供它们的编码器（GIF 取首帧，SVG 先展平再栅格化）。
- 上限：单文件 100 MB（超过 20 MB 时提示），单批 200 个文件。不做视频、音频、电子书与 CAD 格式。

<!-- 下方的链接在标签存在后才可访问；推送 `vX.Y.Z` 后由 .github/workflows/release.yml 创建 Release。 -->

[未发布]: https://github.com/liaolongdong/transfer-any-file/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/liaolongdong/transfer-any-file/releases/tag/v1.0.0
