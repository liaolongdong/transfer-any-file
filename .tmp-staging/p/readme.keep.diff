diff --git a/README.md b/README.md
index 4bdfe1e..9d1376e 100644
--- a/README.md
+++ b/README.md
@@ -190,6 +203,7 @@ _图上可达但语义不成立，因此置灰并说明原因，而不是等你
 - **可中断** — 长批次可中途停止，已完成的文件保留；结果区会明确说明这一批「已取消」，而不是把没跑完的文件当成转换失败
 - **增删文件不丢已有结果** — 往批次里追加文件、或删掉其中某一个，只影响它自己那一条结果：其余文件转好的仍在屏上，选中的目标也一起保留。只有当调整后批次里出现了转不到当前目标的文件，才清空整批并说明原因（混批只提供对全部文件都有效的目标）。换目标格式仍然是整体清空——那一屏结果是上一个目标的产物
 - **图片输出参数** — 目标为 PNG / JPEG / WebP 时，可限制最长边、指定编码质量、给出尽力而为的体积上限，并设定 PDF 源栅格化的清晰度。每一项都要主动设置：一个都不设时编码器完全按原样工作。多步链路上，几何参数（最长边、清晰度）逐步生效，质量与体积只作用在你下载的最后一步——在中间产物上追体积，只会提前丢掉最后一步本来还要用的细节
+- **输出文件名模板** — 结果默认叫 `原名_20260914_153012.pdf`：日期加时间精确到秒，同一天转两批不会互相覆盖。偏好设置里可以换成自己的写法，`{name}`（源文件名去掉后缀）、`{date}`、`{time}`、`{index}`（本批内序号，从 1 起）、`{target}`（目标格式）随取随用，输入框下面实时显示示例。打错的占位符（比如 `{tile}`）按普通文字原样留在名字里并当场提示，而不是悄悄少一截；`/`、`\`、`:`、`*`、`?`、`"`、`<`、`>`、`|` 与控制字符一律清掉，所以模板再离谱也生不出路径。扩展名不归模板管，始终是产物实际的那个（多步链路取最后一步，打包下载就是 `.zip`）；清空输入框回到默认
 
 ### 🕘 历史与个性化
 
@@ -235,7 +249,7 @@ pnpm build
 4. 转成图片时，选择器下方会出现**输出参数**——最长边、质量、体积上限、PDF 清晰度；源里有 PDF 还多一个**页码范围**（如 `1-3, 5`，留空是全部页）；一项都不设就是编码器原本的默认值
 5. 点击 **开始转换**，随后单个下载或整批打包为 ZIP
 6. 每次都做同一套转换？把它存成**预设**，下次在预设卡片里点一下，格式和参数一起回来
-7. 打开右上角 **偏好设置**，可切换主题 / 语言 / 显示模式，开关完成通知与大批次确认，或重新绑定转换快捷键
+7. 打开右上角 **偏好设置**，可切换主题 / 语言 / 显示模式，开关完成通知与大批次确认，改写结果文件名模板（如 `{name}_{index}`），或重新绑定转换快捷键
 
 ## ❓ 常见问题
 
@@ -266,6 +280,9 @@ pnpm build
 **支持哪些浏览器？**
 构建产物是 Chrome Manifest V3 包，在 Edge、Brave 等 Chromium 内核浏览器中同样可以加载。本仓库没有 Firefox 构建。
 
+**结果文件名的时间戳能去掉吗？**
+能。偏好设置里的「输出文件名」默认写法是 `{name}_{date}_{time}`，改成 `{name}` 就得到 `report.pdf` 这样干净的名字。代价是同批里两个文件可能撞到同一个名字——这时后一个自动带 `_2`、`_3` 后缀，这道防线一直在，不管模板里有没有 `{date}`。改动只影响之后下载的产物；历史里记的仍是源文件名，改名不会把某条历史变成搜不到。
+
 **转换历史存在哪，怎么清空？**
 存在本机的 `chrome.storage.local` 中，只包含文件名、格式与体积——绝不保存文件内容。历史面板支持删除单条记录、一键清空，以及把列表导出/导入为 JSON。删错了不必紧张：无论删单条还是清空，提示里 5 秒内都有「撤销」。卸载扩展会一并删除这份本地存储，因为别处没有副本。
 
