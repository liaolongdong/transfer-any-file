diff --git a/docs/index.html b/docs/index.html
index 0ec1ead..c567586 100644
--- a/docs/index.html
+++ b/docs/index.html
@@ -169,7 +169,7 @@
             "alternateName": "Transfer Any File — Offline File Format Converter",
             "softwareVersion": "1.0.0",
             "datePublished": "2026-09-07",
-            "dateModified": "2026-09-21",
+            "dateModified": "2026-09-22",
             "applicationCategory": "UtilitiesApplication",
             "applicationSubCategory": "File Format Converter",
             "keywords": "offline file format converter, file converter chrome extension, markdown to pdf, word to pdf, csv to excel, json to csv, png to webp, svg to png, html to docx, batch file conversion, convert files without uploading, local-first, privacy",
@@ -383,6 +383,54 @@
                   "text": "可以，一次最多 200 个文件。每个文件各自解析自己的转换路径并逐步执行，所以 Markdown、CSV 与 Excel 混在一起投进来、统一转成 HTML 是一条动作。目标格式下拉框只提供对批次内全部文件都可达的格式，因此不会出现选了之后一半文件转不了的组合。"
                 }
               },
+              {
+                "@type": "Question",
+                "name": "How do I convert Markdown to Word — and what happens to its images?",
+                "acceptedAnswer": {
+                  "@type": "Answer",
+                  "text": "Drop the .md in and pick Word: the route runs Markdown → HTML → Word in two steps, and the text lands as real Word paragraphs you can select, rewrite and comment on. What happens to an image depends on how it is written in the source — a data URI travels inside the document, a relative path stays relative (so the image files must sit next to the .docx for Word to show them), and an http(s) image is stripped before packing, because Word must not issue a network request while opening your file. Each route page spells out what it keeps and what it drops."
+                }
+              },
+              {
+                "@type": "Question",
+                "name": "怎么把 Markdown 转成 Word？图片会跟过去吗？",
+                "acceptedAnswer": {
+                  "@type": "Answer",
+                  "text": "把 .md 投进工作台、目标选 Word，会按 Markdown → HTML → Word 两步走完，产出的文字在 Word 里可选中、可改写、可加批注。图片的命运取决于它在源文件里怎么写：以 data URI 内嵌的随文档一起打包；相对路径按相对保留，图片文件要和 .docx 放在同一目录 Word 才显示得出来；指向 http(s) 的外链图片在打包前被剥掉——Word 打开你的文件时不该替你发起网络请求。每条链路各自保留了什么、丢掉了什么，单独写在那条链路的说明页上。"
+                }
+              },
+              {
+                "@type": "Question",
+                "name": "Does Word to Markdown keep the images?",
+                "acceptedAnswer": {
+                  "@type": "Answer",
+                  "text": "Yes, but written into the .md as data URIs, which makes the file noticeably heavier — externalise the images again before committing to a repository. What crosses over is content and hierarchy: headings, lists, emphasis, links and table cells. Whatever Markdown cannot express stays behind, so headers and footers, comments, tracked changes, table-of-contents fields, columns and text boxes do not come, and table merges do not either. Legacy .doc (97-2003 binary) is outside the 14 supported formats, so save it as .docx first."
+                }
+              },
+              {
+                "@type": "Question",
+                "name": "Word 转 Markdown 会保留图片吗？",
+                "acceptedAnswer": {
+                  "@type": "Answer",
+                  "text": "保留，但以 data URI 的形式写进 .md，文件因此明显变大——要提交进 Git 的话，建议转完再把图片外链化。搬过来的是内容与层级：标题、列表、加粗与斜体、链接、表格单元格；Markdown 表达不了的东西不会过来，包括页眉页脚、批注、修订记录、目录域、分栏与文本框，表格的合并信息也不保留。老式 .doc（97-2003 二进制）不在支持的 14 种格式里，先在 Word 里另存为 .docx。"
+                }
+              },
+              {
+                "@type": "Question",
+                "name": "Will Excel to CSV turn 00123 into 123, or a date into 45296?",
+                "acceptedAnswer": {
+                  "@type": "Answer",
+                  "text": "Neither. Cells serialise from the stored value, so a cell formatted \"1,234.50\" still writes 1234.5 and a percentage-displayed 0.25 still writes 0.25. Dates are normalised while reading, to yyyy-mm-dd hh:mm:ss — a date-only cell lands as YYYY-MM-DD — independently of your timezone, which is why a day serial like 45296 never reaches the artifact. Text beginning with = + - @ or a tab is escaped, so Excel cannot re-execute it as a formula on open. A multi-sheet workbook yields one CSV per sheet inside a ZIP."
+                }
+              },
+              {
+                "@type": "Question",
+                "name": "Excel 转 CSV 会不会把 00123 变成 123、把日期变成 45296？",
+                "acceptedAnswer": {
+                  "@type": "Answer",
+                  "text": "既不会输出显示文本，也不会输出序列号。单元格按存储值序列化，所以格式化成 \"1,234.50\" 的那个仍然写 1234.5、按百分比显示的 0.25 仍然写 0.25；日期在读取期统一成 yyyy-mm-dd hh:mm:ss（只含日期的落成 YYYY-MM-DD），不受所在时区影响——这就是 45296 这类日序列不会出现在结果里的原因。以 = + - @ 或制表符开头的文本会加转义，Excel 重新打开时不会把它当公式执行。多个工作表给出每表一份 CSV、一起装进 ZIP。"
+                }
+              },
               {
                 "@type": "Question",
                 "name": "What are the file size and batch limits?",
@@ -1378,6 +1426,43 @@
         margin-left: 6px;
       }
 
+      /* ============ Pair pages ============ */
+      /* Links to the per-pair pages under `convert/`, which `scripts/render-site-pages.mjs` generates
+         from `scripts/conversion-pages/pairs.mjs`. Only route names live here — every claim about what
+         a route keeps or drops belongs on the page that the generator can check against the code. */
+      .pairs {
+        display: grid;
+        grid-template-columns: repeat(auto-fill, minmax(min(100%, 214px), 1fr));
+        gap: 12px;
+      }
+
+      .pair {
+        display: block;
+        padding: 14px 16px;
+        background: #fff;
+        border: 1px solid var(--line);
+        border-radius: 14px;
+        color: var(--ink);
+        font-size: 15px;
+        font-weight: 650;
+        transition:
+          border-color var(--dur-fast) var(--ease-out),
+          box-shadow var(--dur-fast) var(--ease-out);
+      }
+
+      .pair:hover {
+        border-color: var(--brand-line);
+        box-shadow: var(--shadow-md);
+      }
+
+      .pair small {
+        display: block;
+        margin-top: 3px;
+        color: var(--ink-faint);
+        font-size: 12.5px;
+        font-weight: 500;
+      }
+
       /* ============ Sibling tools ============ */
       .sibling-card {
         display: flex;
@@ -3286,6 +3371,7 @@
           <a href="#install"><span lang="zh-CN">安装</span><span lang="en">Install</span></a>
           <a href="#compare"><span lang="zh-CN">对比</span><span lang="en">Compare</span></a>
           <a href="#faq"><span lang="zh-CN">问答</span><span lang="en">FAQ</span></a>
+          <a href="convert/"><span lang="zh-CN">配对详情</span><span lang="en">Conversions</span></a>
           <div class="lang-toggle">
             <button
               type="button"
@@ -4435,6 +4521,104 @@
         </div>
       </section>
 
+      <!-- ================= PAIR PAGES ================= -->
+      <section id="pairs">
+        <div class="wrap">
+          <div class="section-head">
+            <span class="eyebrow-sm">
+              <svg
+                class="sec-ico"
+                aria-hidden="true"
+              >
+                <use href="#i-grid" />
+              </svg>
+              <span lang="zh-CN">按配对查</span><span lang="en">Pair by pair</span>
+            </span>
+            <h2>
+              <span lang="zh-CN">同样是「能转」，每个配对的差别写在各自的页面上</span>
+              <span lang="en">Two pairs can both be "supported" and still differ — each one has its own page</span>
+            </h2>
+            <p class="sub">
+              <span lang="zh-CN"
+                >这些页面不是把上面的话再说一遍：每条链路给出「会保留什么」「不做什么」「实操提示」和该配对特有的问题。页面由
+                <code>scripts/render-site-pages.mjs</code> 从代码里的注册表与
+                <code>scripts/__baseline__/conversion-paths.json</code>
+                生成，链路对不上时生成脚本会直接失败——页面上画的走法就是工作台真正会执行的走法。</span
+              >
+              <span lang="en"
+                >These pages do not repeat the section above: every route gets "what carries over", "what it does not
+                do", working notes and the questions specific to that pair. They are generated by
+                <code>scripts/render-site-pages.mjs</code> from the registry in the code and
+                <code>scripts/__baseline__/conversion-paths.json</code>, and the generator fails when the two disagree —
+                so the chain drawn on a page is the chain the workbench actually runs.</span
+              >
+            </p>
+          </div>
+          <div class="pairs">
+            <a
+              class="pair"
+              href="convert/markdown-to-word.html"
+              >Markdown → Word<small>.md → .docx</small></a
+            >
+            <a
+              class="pair"
+              href="convert/word-to-markdown.html"
+              >Word → Markdown<small>.docx → .md</small></a
+            >
+            <a
+              class="pair"
+              href="convert/markdown-to-pdf.html"
+              >Markdown → PDF<small>.md → .pdf</small></a
+            >
+            <a
+              class="pair"
+              href="convert/word-to-pdf.html"
+              >Word → PDF<small>.docx → .pdf</small></a
+            >
+            <a
+              class="pair"
+              href="convert/pdf-to-text.html"
+              >PDF → Text<small>.pdf → .txt</small></a
+            >
+            <a
+              class="pair"
+              href="convert/excel-to-csv.html"
+              >Excel → CSV<small>.xlsx → .csv</small></a
+            >
+            <a
+              class="pair"
+              href="convert/csv-to-excel.html"
+              >CSV → Excel<small>.csv → .xlsx</small></a
+            >
+            <a
+              class="pair"
+              href="convert/json-to-csv.html"
+              >JSON → CSV<small>.json → .csv</small></a
+            >
+            <a
+              class="pair"
+              href="convert/png-to-webp.html"
+              >PNG → WebP<small>.png → .webp</small></a
+            >
+            <a
+              class="pair"
+              href="convert/svg-to-png.html"
+              >SVG → PNG<small>.svg → .png</small></a
+            >
+          </div>
+          <p class="sub">
+            <a href="convert/"><span lang="zh-CN">看完整转换一览</span><span lang="en">Browse all conversions</span></a>
+            ·
+            <a href="blog/"
+              ><span lang="zh-CN">设计手记：为什么把转换器做成一张图，以及批量场景踩过的坑</span>
+              <span lang="en"
+                >Design notes: why the converters became a graph, and the batch pitfalls that followed</span
+              ></a
+            >
+          </p>
+        </div>
+      </section>
+
       <!-- ================= HOW IT WORKS ================= -->
       <section
         id="how"
@@ -4955,6 +5139,46 @@
                 >
               </p>
             </article>
+            <article class="card reveal">
+              <div class="icon teal">
+                <svg
+                  width="20"
+                  height="20"
+                  viewBox="0 0 24 24"
+                  fill="none"
+                  stroke="currentColor"
+                  stroke-width="2"
+                  stroke-linecap="round"
+                  stroke-linejoin="round"
+                  aria-hidden="true"
+                >
+                  <path d="M8 3H7a2 2 0 0 0-2 2v5a2 2 0 0 1-2 2 2 2 0 0 1 2 2v5c0 1.1.9 2 2 2h1" />
+                  <path d="M16 21h1a2 2 0 0 0 2-2v-5c0-1.1.9-2 2-2a2 2 0 0 1-2-2V5a2 2 0 0 0-2-2h-1" />
+                </svg>
+              </div>
+              <h3><span lang="zh-CN">输出文件名由你定</span><span lang="en">Names you can write yourself</span></h3>
+              <p>
+                <span lang="zh-CN"
+                  >结果默认叫
+                  <code>原名_20260914_153012.pdf</code
+                  >：日期加时间精确到秒，同一天转两批不会互相覆盖。偏好设置里这一串可以改写，<code>{name}</code>
+                  （源文件名去掉后缀）、<code>{date}</code>、<code>{time}</code>、<code>{index}</code>（本批序号）、<code
+                    >{target}</code
+                  >
+                  （目标格式）任意组合，输入框下方实时给出示例。路径分隔符与控制字符会被清掉，扩展名始终由实际产物决定，所以再离谱的模板也写不出路径、写不出说谎的后缀；写错的占位符按普通文字保留并当场提示。这套写法只存在扩展自己的本地存储里。</span
+                >
+                <span lang="en"
+                  >A result is called <code>original_20260914_153012.pdf</code> by default — date plus time to the
+                  second, so two batches on one day never overwrite each other. Preferences lets you rewrite that string
+                  from any mix of <code>{name}</code> (the source name without its extension), <code>{date}</code>,
+                  <code>{time}</code>, <code>{index}</code> (position in the batch) and <code>{target}</code> (the
+                  output format), with a live example under the field. Path separators and control characters get
+                  stripped and the extension always comes from the artifact itself, so no pattern can produce a path or
+                  a suffix that is not true; a mis-typed placeholder stays literal and is pointed out on the spot. The
+                  pattern is written only to the extension's own local storage.</span
+                >
+              </p>
+            </article>
             <article class="card reveal">
               <div class="icon">
                 <svg
@@ -5064,46 +5288,6 @@
                 >
               </p>
             </article>
-            <article class="card reveal">
-              <div class="icon teal">
-                <svg
-                  width="20"
-                  height="20"
-                  viewBox="0 0 24 24"
-                  fill="none"
-                  stroke="currentColor"
-                  stroke-width="2"
-                  stroke-linecap="round"
-                  stroke-linejoin="round"
-                  aria-hidden="true"
-                >
-                  <path d="M8 3H7a2 2 0 0 0-2 2v5a2 2 0 0 1-2 2 2 2 0 0 1 2 2v5c0 1.1.9 2 2 2h1" />
-                  <path d="M16 21h1a2 2 0 0 0 2-2v-5c0-1.1.9-2 2-2a2 2 0 0 1-2-2V5a2 2 0 0 0-2-2h-1" />
-                </svg>
-              </div>
-              <h3><span lang="zh-CN">输出文件名由你定</span><span lang="en">Names you can write yourself</span></h3>
-              <p>
-                <span lang="zh-CN"
-                  >结果默认叫
-                  <code>原名_20260914_153012.pdf</code
-                  >：日期加时间精确到秒，同一天转两批不会互相覆盖。偏好设置里这一串可以改写，<code>{name}</code>
-                  （源文件名去掉后缀）、<code>{date}</code>、<code>{time}</code>、<code>{index}</code>（本批序号）、<code
-                    >{target}</code
-                  >
-                  （目标格式）任意组合，输入框下方实时给出示例。路径分隔符与控制字符会被清掉，扩展名始终由实际产物决定，所以再离谱的模板也写不出路径、写不出说谎的后缀；写错的占位符按普通文字保留并当场提示。这套写法只存在扩展自己的本地存储里。</span
-                >
-                <span lang="en"
-                  >A result is called <code>original_20260914_153012.pdf</code> by default — date plus time to the
-                  second, so two batches on one day never overwrite each other. Preferences lets you rewrite that string
-                  from any mix of <code>{name}</code> (the source name without its extension), <code>{date}</code>,
-                  <code>{time}</code>, <code>{index}</code> (position in the batch) and <code>{target}</code> (the
-                  output format), with a live example under the field. Path separators and control characters get
-                  stripped and the extension always comes from the artifact itself, so no pattern can produce a path or
-                  a suffix that is not true; a mis-typed placeholder stays literal and is pointed out on the spot. The
-                  pattern is written only to the extension's own local storage.</span
-                >
-              </p>
-            </article>
             <article class="card reveal">
               <div class="icon">
                 <svg
@@ -6287,6 +6471,120 @@ pnpm build</code><button
                 >
               </div>
             </details>
+            <details class="faq-item">
+              <summary>
+                <span lang="zh-CN">怎么把 Markdown 转成 Word？图片会跟过去吗？</span>
+                <span lang="en">How do I convert Markdown to Word — and what happens to its images?</span>
+                <svg
+                  class="chev"
+                  width="17"
+                  height="17"
+                  viewBox="0 0 24 24"
+                  fill="none"
+                  stroke="currentColor"
+                  stroke-width="2.4"
+                  stroke-linecap="round"
+                  stroke-linejoin="round"
+                  aria-hidden="true"
+                >
+                  <path d="m9 6 6 6-6 6" />
+                </svg>
+              </summary>
+              <div class="faq-a">
+                <span lang="zh-CN"
+                  >把 .md 投进工作台、目标选 Word，会按 Markdown → HTML → Word 两步走完，产出的文字在 Word
+                  里可选中、可改写、可加批注。图片的命运取决于它在源文件里怎么写：以 data URI
+                  内嵌的随文档一起打包；相对路径按相对保留，图片文件要和 .docx 放在同一目录 Word 才显示得出来；指向
+                  http(s) 的外链图片在打包前被剥掉——Word
+                  打开你的文件时不该替你发起网络请求。每条链路各自保留了什么、丢掉了什么，单独写在
+                  <a href="convert/markdown-to-word.html">Markdown 转 Word</a> 那一页。</span
+                >
+                <span lang="en"
+                  >Drop the .md in and pick Word: the route runs Markdown → HTML → Word in two steps, and the text lands
+                  as real Word paragraphs you can select, rewrite and comment on. What happens to an image depends on
+                  how it is written in the source — a data URI travels inside the document, a relative path stays
+                  relative (so the image files must sit next to the .docx for Word to show them), and an http(s) image
+                  is stripped before packing, because Word must not issue a network request while opening your file.
+                  What each route keeps and drops is spelled out on its own page, here
+                  <a href="convert/markdown-to-word.html">Markdown to Word</a>.</span
+                >
+              </div>
+            </details>
+            <details class="faq-item">
+              <summary>
+                <span lang="zh-CN">Word 转 Markdown 会保留图片吗？</span>
+                <span lang="en">Does Word to Markdown keep the images?</span>
+                <svg
+                  class="chev"
+                  width="17"
+                  height="17"
+                  viewBox="0 0 24 24"
+                  fill="none"
+                  stroke="currentColor"
+                  stroke-width="2.4"
+                  stroke-linecap="round"
+                  stroke-linejoin="round"
+                  aria-hidden="true"
+                >
+                  <path d="m9 6 6 6-6 6" />
+                </svg>
+              </summary>
+              <div class="faq-a">
+                <span lang="zh-CN"
+                  >保留，但以 data URI 的形式写进 .md，文件因此明显变大——要提交进 Git
+                  的话，建议转完再把图片外链化。搬过来的是内容与层级：标题、列表、加粗与斜体、链接、表格单元格；Markdown
+                  表达不了的东西不会过来，包括页眉页脚、批注、修订记录、目录域、分栏与文本框，表格的合并信息也不保留。老式
+                  .doc（97-2003 二进制）不在支持的 14 种格式里，先在 Word 里另存为 .docx。细节见
+                  <a href="convert/word-to-markdown.html">Word 转 Markdown</a>。</span
+                >
+                <span lang="en"
+                  >Yes, but written into the .md as data URIs, which makes the file noticeably heavier — externalise the
+                  images again before committing to a repository. What crosses over is content and hierarchy: headings,
+                  lists, emphasis, links and table cells. Whatever Markdown cannot express stays behind, so headers and
+                  footers, comments, tracked changes, table-of-contents fields, columns and text boxes do not come, and
+                  table merges do not either. Legacy .doc (97-2003 binary) is outside the 14 supported formats, so save
+                  it as .docx first. Details on the
+                  <a href="convert/word-to-markdown.html">Word to Markdown</a> page.</span
+                >
+              </div>
+            </details>
+            <details class="faq-item">
+              <summary>
+                <span lang="zh-CN">Excel 转 CSV 会不会把 00123 变成 123、把日期变成 45296？</span>
+                <span lang="en">Will Excel to CSV turn 00123 into 123, or a date into 45296?</span>
+                <svg
+                  class="chev"
+                  width="17"
+                  height="17"
+                  viewBox="0 0 24 24"
+                  fill="none"
+                  stroke="currentColor"
+                  stroke-width="2.4"
+                  stroke-linecap="round"
+                  stroke-linejoin="round"
+                  aria-hidden="true"
+                >
+                  <path d="m9 6 6 6-6 6" />
+                </svg>
+              </summary>
+              <div class="faq-a">
+                <span lang="zh-CN"
+                  >既不会输出显示文本，也不会输出序列号。单元格按存储值序列化，所以格式化成 "1,234.50" 的那个仍然写
+                  1234.5、按百分比显示的 0.25 仍然写 0.25；日期在读取期统一成 yyyy-mm-dd hh:mm:ss（只含日期的落成
+                  YYYY-MM-DD），不受所在时区影响——这就是 45296 这类日序列不会出现在结果里的原因。以 = + - @
+                  或制表符开头的文本会加转义，Excel 重新打开时不会把它当公式执行。多个工作表给出每表一份 CSV、一起装进
+                  ZIP。见 <a href="convert/excel-to-csv.html">Excel 转 CSV</a>。</span
+                >
+                <span lang="en"
+                  >Neither. Cells serialise from the stored value, so a cell formatted "1,234.50" still writes 1234.5
+                  and a percentage-displayed 0.25 still writes 0.25. Dates are normalised while reading, to yyyy-mm-dd
+                  hh:mm:ss (a date-only cell lands as YYYY-MM-DD), independent of your timezone — which is why a day
+                  serial like 45296 never reaches the artifact. Text beginning with = + - @ or a tab is escaped, so
+                  Excel cannot re-execute it as a formula on open. A multi-sheet workbook yields one CSV per sheet
+                  inside a ZIP. See <a href="convert/excel-to-csv.html">Excel to CSV</a>.</span
+                >
+              </div>
+            </details>
             <h3 class="faq-cat"><span lang="zh-CN">使用与限制</span><span lang="en">Usage &amp; limits</span></h3>
             <details class="faq-item">
               <summary>
@@ -6680,16 +6978,17 @@ pnpm build</code><button
               <p>
                 <span lang="zh-CN"
                   >一套 Playwright 端到端套件加载真正的构建产物，用仓库内的夹具跑完各类转换场景、批量与
-                  ZIP、取消与改键、预览与编辑、图片输出参数与转换预设、PDF 选页、历史与偏好，共 303
-                  项断言全部通过（1.0.0 之后的未发布构建，2026-09-24 实测）；另在 6 主题 × 浅/深共 12
+                  ZIP、取消与改键、预览与编辑、图片输出参数与转换预设、PDF 选页、输出文件名模板、历史与偏好，共
+                  313 项断言全部通过（1.0.0 之后的未发布构建，2026-09-24 实测）；另在 6 主题 × 浅/深共 12
                   种组合下逐项校验四组对比度：顶栏文字、主按钮文字（常态 / 悬停 / 按下）、焦点环与正文信息文字。CI
                   每次推送跑 lint + build + e2e。</span
                 >
                 <span lang="en"
                   >A Playwright end-to-end suite drives the actual build output over the repository fixtures — every
                   conversion scenario, batching and ZIP handling, cancellation and shortcut rebinding, preview and
-                  editing, image output parameters and conversion presets, PDF page selection, history and preferences —
-                  with 303/303 assertions passing (the unreleased build after 1.0.0, measured 2026-09-24). It also
+                  editing, image output parameters and conversion presets, PDF page selection, output file-name
+                  patterns, history and preferences — with 313/313 assertions passing (the unreleased build after 1.0.0,
+                  measured 2026-09-24). It also
                   checks four contrast pairs — topbar text, the primary button's label in its rest, hover and pressed
                   states, the focus ring, and informational text such as the drop hint and file sizes — in all 12 theme
                   × light/dark combinations. CI runs lint, build and e2e on every push.</span
@@ -6714,19 +7013,21 @@ pnpm build</code><button
           </div>
           <p class="updated">
             <span lang="zh-CN"
-              ><strong>版本 1.0.0 · 本页最后更新 2026-09-21。</strong>文中全部数字从代码取证：14 种格式来自
+              ><strong>版本 1.0.0 · 本页最后更新 2026-09-22。</strong>文中全部数字从代码取证：14 种格式来自
               <code>FileFormat</code> 枚举，48 条路径与 143 个可达组合来自转换器注册表的邻接图，116 个可选组合是 143
               减去 <code>utils/core/conversion-policy.ts</code> 置灰的 27 个，3.76 MB 来自
               <code>pnpm build</code> 输出，权限与上限来自 manifest 与源码常量。截图与推广图由
-              <code>pnpm assets:capture</code> 从构建产物生成。</span
+              <code>pnpm assets:capture</code> 从构建产物生成；配对详情页由
+              <code>pnpm pages:render</code> 生成，这一批共 11 个页面，与 <code>docs/sitemap.xml</code> 同步。</span
             >
             <span lang="en"
-              ><strong>Version 1.0.0 · page last updated 2026-09-21.</strong> Every number on this page is taken from
+              ><strong>Version 1.0.0 · page last updated 2026-09-22.</strong> Every number on this page is taken from
               the code: 14 formats from the <code>FileFormat</code> enum, 48 routes and 143 reachable combinations from
               the converter registry's adjacency graph, and 116 selectable combinations — that is 143 minus the 27 pairs
               <code>utils/core/conversion-policy.ts</code> greys out. 3.76 MB comes from <code>pnpm build</code> output,
               permissions and limits from the manifest and source constants. Screenshots are generated from the built
-              bundle by <code>pnpm assets:capture</code>.</span
+              bundle by <code>pnpm assets:capture</code>, and the per-pair pages — 11 pages in this batch — come from
+              <code>pnpm pages:render</code>, which writes the sitemap they are listed in.</span
             >
           </p>
         </div>
@@ -6982,6 +7283,8 @@ pnpm build</code><button
             <a href="#compare"><span lang="zh-CN">横向对比</span><span lang="en">Comparison</span></a>
             <a href="#privacy"><span lang="zh-CN">隐私与权限</span><span lang="en">Privacy &amp; permissions</span></a>
             <a href="#faq"><span lang="zh-CN">常见问题</span><span lang="en">FAQ</span></a>
+            <a href="convert/"><span lang="zh-CN">配对详情</span><span lang="en">Conversions</span></a>
+            <a href="blog/"><span lang="zh-CN">设计手记</span><span lang="en">Design notes</span></a>
             <a href="#details"><span lang="zh-CN">技术细节</span><span lang="en">Technical details</span></a>
             <a href="#more"><span lang="zh-CN">同作者工具</span><span lang="en">More by the author</span></a>
             <a href="#contact"><span lang="zh-CN">联系</span><span lang="en">Contact</span></a>
@@ -7032,7 +7335,7 @@ pnpm build</code><button
               </a>
             </span>
             <span>
-              <span lang="zh-CN">最后更新 2026-09-21 · </span><span lang="en">Last updated 2026-09-21 · </span
+              <span lang="zh-CN">最后更新 2026-09-22 · </span><span lang="en">Last updated 2026-09-22 · </span
               ><a
                 href="https://github.com/liaolongdong/transfer-any-file/blob/main/CHANGELOG.md"
                 rel="noopener"
