diff --git a/CHANGELOG.md b/CHANGELOG.md
index 77f73a8..324ee59 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -10,6 +10,18 @@ manifest 的版本号，因此 `vX.Y.Z` 标签、构建产物与商店包始终
 
 ### 新增
 
+- **结果文件名可以自己写了。** 偏好设置新增「输出文件名」一栏，默认写法 `{name}_{date}_{time}` 渲染出的正是
+  此前硬码在编排层的那串名字（`report_20260914_153012.pdf`），所以从没碰过这一栏的用户拿到的字节与昨天完全相同。
+  占位符五个：`{name}` 源文件名去掉后缀、`{date}` `20260914`、`{time}` `153012`、`{index}` 本批序号（从 1 起）、
+  `{target}` 目标格式。三条不显然的：**扩展名不由模板管**，它由最后一步的实际产物决定（`containerExt` 说
+  `.zip` 就是 `.zip`）——预览的「可编辑文本」判定、结果卡片上的格式徽标、ZIP 里逐条目的压缩策略都是从名字末尾
+  那个点往回读的，让模板去写扩展名等于给这三处制造说谎的机会；**`{name}` 最后替换**，于是一份真名叫
+  `{date}.md` 的文件不可能往别人的名字里注入一个令牌；**这一栏与 storage 一样是不可信输入**，`/`、`\`、`:`、
+  `*`、`?`、`"`、`<`、`>`、`|`、控制字符在存进去之前清一遍、每个被替换进来的值上再清一遍、整串渲染结果最后还清一遍，
+  于是「手工改过的 store」和「Windows 造的 ZIP 条目名带反斜杠」这两条路堵在同一个地方。打错的占位符（`{tile}`）
+  按普通文字留在名字里，并在输入框下方点名提示——悄悄丢掉它等于把文件改名成 `report-` 却一个字不提。清空输入框
+  回到默认，用户不必准确重打那串默认值。同名碰撞的 `_2` 后缀原样保留，而在 `{name}` 这种不带时间戳的写法下
+  它是唯一防线，所以 e2e 用 `notes.csv` + `notes.json` 这一对撞名显式钉住两条结果都到场。
 - **拖入文件夹即整批导入。** 此前拖进来一个文件夹等于什么都没发生，唯一的绕法是先压成 `.zip`
   再拖——而那条路本来就能解包。现在文件夹按广度优先递归展开，只收识别表里认得的格式与 `.zip`，
   隐藏文件与 `__MACOSX` 跳过，收满一批的上限即停。三条不显然的：`readEntries()` 每次只给一批，
@@ -188,9 +200,23 @@ manifest 的版本号，因此 `vX.Y.Z` 标签、构建产物与商店包始终
   （build 之前）跑；默认那层读 `.output/chrome-mv3`，缺产物即失败。已接进 CI 的 lint 与 build 两个 job，
   以及发布工作流里 **Package 之前**那一步。加它的原因见同段「修复」里那条：这类形状只存在于第三方压缩产物里，
   「源码里没有网络请求」与「manifest 只要 storage」两条断言都从结构上看不见它。
+- **站点多了一层「一条链路一页」的内容，而且它是生成的。** `docs/convert/` 现在是 1 张索引加 10 张配对说明页
+  （Markdown→Word、Word→Markdown、Excel⇄CSV、JSON→CSV、PDF→文本、Markdown→PDF、Word→PDF、PNG→WebP、SVG→PNG），
+  `docs/blog/` 把原本只躺在 `docs/promo/` 的那篇长文变成真正发布的页面。文案的唯一来源是
+  `scripts/conversion-pages/pairs.mjs`，`scripts/render-site-pages.mjs` 渲染它们并顺手重写 `docs/sitemap.xml`
+  （现在 14 条 URL）：改一个句子只改数据源，`pnpm pages:render` 重生成，`pnpm pages:check` 比对已提交字节。
+  每条链路「保留什么、丢什么」向路径基线与转换器实现取证，不写成营销句；双语仍由 CSS 按 `lang` 属性显隐，
+  所以不跑 JavaScript 的抓取器读到的是同一批事实。产品页另加 3 条长尾 FAQ，分别回答「Markdown 进 Word 时图片
+  会怎样」「Word 出 Markdown 时图片变成 data URI 吗」「Excel 出 CSV 是值还是显示文本」，JSON-LD 的 `Question`
+  与可见条目维持 1:1（22 问 × 2 语言）。这两类生成物因此退出 Prettier 的管辖——手改会被下一次渲染覆盖——
+  守卫换成 `pages:check`，已接进 CI 的 lint job，而 `verify:numbers` 把它们一起纳入取证范围。
 
 ### 变更
 
+- **打包下载那个 ZIP 的文件名跟着走新规则。** 原先是 `converted-<日期_时间>.zip`，现在与单文件同一个模板，
+  默认写法下得到 `converted_<日期_时间>.zip`——连字符换成下划线，一个字符的漂移。没有为它保留旧格式：
+  「压缩包名字另用一套规则」正是这个功能要消掉的特例，而它只影响下载目录里那一个名字，不影响存储结构、
+  也不影响 ZIP 内部的条目名（那仍是各结果自己的名字）。
 - **增删文件不再丢掉与这次无关的已有结果。** 往批次里追加一个文件、或点某行的删除按钮拿走一个文件，
   原先会把整屏结果连同已选目标一起清空——删掉一个转错的文件，代价是其余每一个都要重跑。现在只有
   「这次调整让当前目标对批次里某个文件不再成立」才清空，而且会明确说一句为什么清空（混批只提供对全部
