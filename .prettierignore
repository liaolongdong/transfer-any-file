# Generated or vendored — never hand-formatted.
pnpm-lock.yaml
# Rewritten by `wxt prepare` / unplugin-vue-components on every build.
auto-imports.d.ts
components.d.ts
.output/
dist/
node_modules/
.test-screenshots/

# Test fixtures are byte-exact conversion inputs; the e2e suite asserts on their
# sizes and contents, so reformatting them changes the measurements, not the style.
fixtures/

# Agent-facing specs and plans: prose documents that gain nothing from a reflow diff.
.qoder/

# Generated site pages and the sitemap that lists them. `pnpm pages:render` rewrites them from
# scripts/conversion-pages/pairs.mjs, so a hand format would be overwritten on the next run;
# `pnpm pages:check` compares the committed bytes to what the data source produces, which is a stronger
# guard than formatting coverage here.
docs/convert/
docs/sitemap.xml
