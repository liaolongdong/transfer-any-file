node_modules
build
dist
.cache
.output
.wxt
# GitHub Pages staging directory (see .github/workflows/static.yml)
_site/
*.local
# Credentials for `wxt-publish-extension` (Chrome Web Store / addons.mozilla.org)
.env.submit
.test-files
.test-screenshots
.tmp-e2e-profile
# Generated paste-ready drafts (images inlined as data URIs, ~MBs each); the .md sources are tracked.
docs/promo/*.html
.DS_Store
